{
  "required": [
    "position"
  ],
  "properties": {
    "type": {
      "enum": [
        "fixedPosition"
      ],
      "type": "string"
    },
    "position": {
      "type": "integer",
      "description": "One-based result position assigned to matching entities.",
      "format": "int32"
    }
  },
  "description": "Configuration for a rule that places matching entities at a fixed result position."
}
