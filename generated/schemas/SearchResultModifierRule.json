{
  "required": [
    "id",
    "isApproved",
    "scope",
    "condition",
    "actions"
  ],
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "The stable Rule ID.",
      "format": "uuid"
    },
    "isApproved": {
      "type": "boolean",
      "description": "Whether the rule is approved and active for matching searches."
    },
    "scope": {
      "allOf": [
        {
          "$ref": "#/components/schemas/SearchToolRuleScope"
        }
      ],
      "description": "Search Index and language scope in which the rule applies."
    },
    "condition": {
      "allOf": [
        {
          "$ref": "#/components/schemas/SearchTermCondition"
        }
      ],
      "description": "Complete condition that activates the rule."
    },
    "actions": {
      "type": "array",
      "items": {
        "$ref": "#/components/schemas/SearchResultModifierAction"
      },
      "description": "Ordered actions applied to Search results."
    }
  },
  "description": "Detailed Search Result Modifier rule representation including its complete condition and actions."
}
