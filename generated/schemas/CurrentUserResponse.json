{
  "required": [
    "user",
    "authentication",
    "licenses"
  ],
  "type": "object",
  "properties": {
    "user": {
      "allOf": [
        {
          "$ref": "#/components/schemas/UserDetails"
        }
      ],
      "description": "The authenticated My Relewise user."
    },
    "authentication": {
      "allOf": [
        {
          "$ref": "#/components/schemas/AuthenticationMetadata"
        }
      ],
      "description": "The authentication method, display name, and non-secret credential details."
    },
    "licenses": {
      "type": "array",
      "items": {
        "$ref": "#/components/schemas/LicenseReference"
      },
      "description": "Licenses containing Datasets visible to the authenticated user and allowed by the current credential Dataset selection. Verify a Dataset with CoreGetDataset before using it; the collection is empty when there are no datasets."
    }
  },
  "description": "The authenticated My Relewise user, non-secret authentication metadata, and License-grouped Dataset references available for access verification."
}
