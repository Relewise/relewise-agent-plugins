{
  "required": [
    "kind",
    "value",
    "minimumLength",
    "negated",
    "and",
    "or"
  ],
  "type": "object",
  "properties": {
    "kind": {
      "type": "string",
      "description": "The matching kind, such as Equals, Contains, StartsWith, or EndsWith.",
      "nullable": true
    },
    "value": {
      "type": "string",
      "description": "The text that the submitted search term is compared against. Depending on Kind, the search term must equal, contain, start with, or end with this value.",
      "nullable": true
    },
    "minimumLength": {
      "pattern": "^-?(?:0|[1-9]\\d*)$",
      "description": "Optional minimum search-term length required for this condition.",
      "format": "int32",
      "nullable": true,
      "type": "integer"
    },
    "negated": {
      "type": "boolean",
      "description": "Whether the condition result is inverted."
    },
    "and": {
      "type": "array",
      "items": {
        "$ref": "#/components/schemas/SearchTermCondition"
      },
      "description": "Conditions that must all match in addition to this condition."
    },
    "or": {
      "type": "array",
      "items": {
        "$ref": "#/components/schemas/SearchTermCondition"
      },
      "description": "Conditions of which at least one must match in addition to this condition."
    }
  },
  "description": "A condition that matches a search term. Nested conditions are combined with the selected logical collections."
}
