{
  "required": [
    "currency",
    "amount"
  ],
  "type": "object",
  "properties": {
    "currency": {
      "type": "string",
      "description": "Currency identifier stored with the price. It is not guaranteed to be an ISO 4217 currency code."
    },
    "amount": {
      "pattern": "^-?(?:0|[1-9]\\d*)(?:\\.\\d+)?$",
      "description": "Monetary amount in Currency.",
      "format": "double"
    }
  },
  "description": "One monetary amount in a specific currency."
}
