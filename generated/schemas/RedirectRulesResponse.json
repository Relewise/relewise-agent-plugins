{
  "required": [
    "rules",
    "pagination"
  ],
  "type": "object",
  "properties": {
    "rules": {
      "type": "array",
      "items": {
        "$ref": "#/components/schemas/RedirectRuleSummary"
      },
      "description": "Compact Redirect rule representations returned on this page."
    },
    "pagination": {
      "$ref": "#/components/schemas/PaginationMetadata"
    }
  },
  "description": "A bounded page of Redirect rules."
}
